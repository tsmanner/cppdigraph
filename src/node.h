/*
 *
 */

#ifndef CDG_NODE_H
#define CDG_NODE_H

#include <string>
#include <unordered_map>

#include "edge.h"


class CdgNode {
public:
  CdgNode();

//   virtual void addEdge(Edge* edge) {
//     if (edge->getHead() == this) mHeads[edge.getName()].push_back(edge);
//     else if (edge->getTail() == this) mTails[edge.getName()].push_back(edge);
//   }

private:
//   unordered_map<string, list<Edge*>> mHeads;
//   unordered_map<string, list<Edge*>> mTails;

};

#endif
